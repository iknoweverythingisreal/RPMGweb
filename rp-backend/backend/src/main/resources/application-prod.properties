# ===== PRODUCTION PROFILE =====
# Activate with: SPRING_PROFILES_ACTIVE=prod  (or --spring.profiles.active=prod)
# Values here override application.properties only when the prod profile is active.

# DataSource (new production database)
spring.datasource.url=jdbc:postgresql://187.127.118.69:5432/accpro_db
spring.datasource.username=accpro_user
spring.datasource.password=Accpro_Pg!2026

# Quieter logs in production
spring.jpa.show-sql=false
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO

# First boot against an empty database: Flyway creates the schema automatically.
# If the database already has tables NOT created by Flyway, uncomment this once:
# spring.flyway.baseline-on-migrate=true
